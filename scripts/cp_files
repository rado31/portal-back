cp_files() {
	eval rsync -ah --info=progress2 "$SOURCE_PATH/changes/movies/*" "$TARGET_PATH/movies"
	eval rsync -ah --info=progress2 "$SOURCE_PATH/changes/musics/*" "$TARGET_PATH/musics"
	eval rsync -ah --info=progress2 "$SOURCE_PATH/changes/books/*" "$TARGET_PATH/books"
}
